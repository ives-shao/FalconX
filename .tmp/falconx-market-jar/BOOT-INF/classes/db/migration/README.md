# market-service migration

当前目录预留给 `falconx-market-service` 的 Flyway migration。

Stage 1 只建立目录骨架。

后续实现时：

- 这里只放 `falconx_market` 的 MySQL migration
- ClickHouse migration 由 market-service 独立管理，不与 MySQL migration 混放
