# FalconX 当前开发计划

## 1. 当前项目判断

- FalconX `v1` 采用 `GODSA`，一期固定服务为 `gateway / identity-service / market-service / trading-core-service / wallet-service`。
- `Stage 1-5` 已完成：owner MySQL、Flyway、MyBatis + XML、Redis、ClickHouse、Kafka 与最小北向链路已切到真实实现。
- `Stage 6A` 主链路收口已完成，当前正式执行阶段进入：`Stage 6B 用户侧实时与运营完整性`。
- `Stage 6B` 当前处于首批实现阶段；`Stage 6C / 7 / 7A` 仍未进入验收完成状态。
- 当前系统仍不能表述为“生产可用”或“可安全对外公测”。

## 2. 基线与分支结论

### 2.1 分支差异结论

- `origin/main` 是本轮唯一真源。
- `main` 当前基线为 `a5faa35`，与 `origin/main` 一致。
- `main / origin/main` 仅落后一个 merge commit，功能树 diff 为空；本轮不从旧分支回收功能代码。
- 本轮实施分支基于 `origin/main` 创建，禁止把旧分支上的过时代码口径带入当前阶段结论。

### 2.2 主文档冲突矩阵

| 主题 | 冲突前现状 | 本轮统一口径 |
|---|---|---|
| 当前正式阶段 | `README / 开发启动手册` 曾固定在 `Stage 6A`，而 `Swap` 已进入实现 | 当前正式阶段统一为 `Stage 6B 用户侧实时与运营完整性` |
| `Stage 6B` 定义 | 存在“安全完整化”和“用户侧实时与运营完整性”两套口径 | `Stage 6B` 统一为“用户侧实时与运营完整性”；当前只启动 `Swap` 首批实现，不等于整阶段验收完成 |
| `Stage 7 / 7A` 状态 | 旧文档与超前实现事实混用，易误判为已验收 | 本轮不把任何 `Stage 7 / 7A` 事实写成当前阶段完成结论 |
| 逐仓状态 | 文档与代码事实存在错位 | 逐仓相关超前实现不计入本轮阶段完成，`Stage 7A` 保持冻结 |
| `trading` 平仓 / TP/SL / 强平口径 | 数据库设计、README、代码注释曾混用 `Stage 7` 表述 | 统一为：这些是当前实现事实，只用于 `Stage 6A` 交易链路核对，不作为 `Stage 7 / 7A` 验收结论 |
| `TradingHedgeAlertEvent` 定位 | 统一接口文档中容易被理解为正式对外能力 | 统一为：仅服务内 Spring Event stub，不计入 `Stage 6A` 验收，也不代表真实 A-book 对冲已接入 |
| 代表性 E2E 边界 | `统一接口文档` 曾只写 `gateway + identity + trading + Kafka` | 统一写为 `gateway + identity + market + trading + wallet + Kafka` 的受控真运行时联调 |
| `wallet` 单入口边界 | 文档要求单入口，代码保留多链装配 | 统一口径为：本轮仅配置限制 `ETH` 验证，不表述为代码已单链化 |
| `Swap / 隔夜利息` | 文档中既被放进 `Stage 6A`，又被带进后续阶段 | 当前统一为：产品规则已冻结，`Stage 6B` 已启动首批实现；查询展示、业务事件与整体验收仍未完成 |

## 3. 阶段完成概览

- `Stage 1` 已完成：共享模块、多模块父工程、5 个服务与 contract 基础结构已落地。
- `Stage 2A` 已完成：`market-service` 市场数据底座已落地。
- `Stage 2B` 已完成：`wallet-service` 钱包原始事实底座已落地。
- `Stage 3A` 已完成：`identity-service` 身份域基础闭环已落地。
- `Stage 3B` 已完成：`trading-core-service` 交易核心基础闭环已落地。
- `Stage 4` 已完成：`gateway` 与各服务的最小北向链路已打通。
- `Stage 5` 已完成：owner MySQL、Flyway、MyBatis + XML、Redis、Redisson、ClickHouse、Kafka 已切到真实实现。
- `Stage 6A` 已完成：主链路收口证据已成立。
- `Stage 6B` 部分完成：当前是唯一正式执行阶段。
- `Stage 6C / 7 / 7A` 当前冻结，待 `Stage 6B` 当前范围完成后再重新确认顺序与边界。

## 4. 当前仓库事实摘要

说明：

- 下列内容是 `origin/main` 上经代码与文档抽查确认的仓库事实，不等于对应阶段已经验收完成。
- 当前阶段完成判定以 `Stage 6B` 当前范围为准，不把 `Stage 6C / 7 / 7A` 的超前代码事实直接表述为“阶段已完成”。

### 已落地的仓库事实

- `identity-service`
  - 已提供 `register / login / refresh / logout`
  - 已消费 `falconx.trading.deposit.credited` 推进用户激活
  - 已具备登录失败锁定、Access Token 黑名单与 `logout -> blacklist -> gateway reject` 基础能力
- `gateway`
  - 已建立 JWT 鉴权、`traceId` 透传、统一 fallback、限流、熔断与超时配置
- `market-service`
  - 已接入 Tiingo JDK WebSocket 真连接代码路径
  - 已补本地 TLS inspection 场景下的 Tiingo trust store 配置与稳定收流证据
  - 已支持 owner `t_symbol.status=1` 白名单热刷新
  - 已落地最新价缓存、`quote_tick`、真实 K 线收盘聚合、ClickHouse 写入与 `market.kline.update` Outbox 投递
- `wallet-service`
  - 已落地钱包地址分配、owner 持久化、EVM 原生币与 ERC20 最小扫块识别路径
  - 已支持确认窗口重扫、reversal 观察与 `walletTxId` 稳定主键输出
- `trading-core-service`
  - 已落地 `wallet.deposit.confirmed / reversed` 与 `market.price.tick` 的正式消费入口
  - 已具备 `walletTxId` 幂等、Outbox / Inbox 基础链路
  - 已补 `market.price.tick` 的 Kafka 入口失败重试能力
  - 已形成 `market.kline.update` 在 `trading-core-service` 侧的正式消费与 `t_inbox` 留痕
  - 已核对账户 / 持仓 / 风险计算链路，以及手动平仓、TP/SL、强平的当前运行口径；这些事实只用于 `Stage 6A` 收口，不构成 `Stage 7 / 7A` 验收结论

## 5. Stage 6A 已完成项 / Stage 6B 当前范围

### Stage 6A 已完成项

- `market-service`：Tiingo 真连接、owner 白名单热刷新、stale 动态判定、真实 K 线聚合、ClickHouse 写入、`market.kline.update` Outbox 与 Tiingo 受控外部自动化入口已完成。
- `wallet-service`：真实 RPC 最小扫块、`t_wallet_chain_cursor` 回写、确认窗口回扫、原生币识别、reversal、`walletTxId` 与受控外部真节点自动化入口已完成。
- `trading-core-service`：`wallet.deposit.confirmed / reversed` 消费、`walletTxId` 幂等、Outbox / Inbox、`market.price.tick` 主消费链路、Kafka 入口失败重试与 `market.kline.update` 正式消费已完成。
- 代表性 E2E：`TC-E2E-001 / 010 / 011` 已纳入 `gateway + identity-service + market-service + trading-core-service + wallet-service + Kafka` 的受控真运行时联调。

### Stage 6B 当前已落地项

- `market-service`
  - 已落地 `t_swap_rate` owner 读取、按 `symbol` 聚合的 Redis 共享快照、启动预热与每日 `UTC 00:00` 刷新。
  - `Swap rate` 共享 key 固定为 `falconx:market:swap-rate:{symbol}`。
- `trading-core-service`
  - 已落地 owner Redis 快照读取、本地定时 `rollover` 扫描、`t_ledger.biz_type=6/7` 幂等落账与 `swap:{positionId}:{rolloverAt}` 账本主幂等键。
  - 已按 `BUY -> bid / SELL -> ask` 与 `rollover ± stale.max-age` 窗口内 fresh 报价完成首版结算。
  - 已补 `TradingSwapSettlementIntegrationTests`，覆盖多头收取、空头收入、stale 跳过后重试与幂等。
- 验证证据
  - `mvn -pl falconx-market-service,falconx-trading-core-service -am -DskipTests compile` 通过。
  - `mvn -pl falconx-market-service -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=MarketInfrastructureIntegrationTests test` 通过。
  - `mvn -pl falconx-trading-core-service -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=TradingSwapSettlementIntegrationTests test` 通过。

### 当前未完成项

- Tiingo 外部真源自动化入口已补齐，但当前环境仍缺显式 `API key / trust store` 运行证据。
- 外部链节点真扫块自动化入口已补齐，但当前环境仍缺显式 `ETH RPC / JVM trust store` 运行证据。
- `wallet-service` 本轮只按配置限制 `ETH` 验证；当前代码仍保留多链装配事实。
- `Swap / 隔夜利息` 的查询展示、业务事件、北向 WebSocket 与更完整运营观测仍未完成。
- 当前 `Swap` 首版严格依赖 `rollover ± stale.max-age` 窗口内 fresh 报价；若未来需要长时间中断后的精确历史补算，仍需单独冻结 `rollover` 历史价格事实来源。
- `TradingHedgeAlertEvent` 仍只是服务内 Spring Event stub，不能表述为真实对冲出口或 `Stage 7` 正式能力。

## 6. Stage 6B 当前执行边界

- 当前阶段名称：`Stage 6B 用户侧实时与运营完整性`
- 当前阶段目标：在不破坏既有 `Stage 6A / Stage 7` E2E 基线的前提下，先完成 `Swap` 首批 owner 共享与本地结算链路。
- 本轮固定口径：
  - `market-service` 继续作为 `Swap rate` owner。
  - `trading-core-service` 继续作为 `Swap` 结算 owner。
  - 禁止跨服务 DB 直连。
  - 首版 `Swap` 不预设新增查询接口，不预设新增 Kafka 业务事件。
  - `wallet-service` 仍仅按配置限制 `ETH` 验证，不做代码层单链收缩。
- 本轮不做：
  - `Stage 6C / 7 / 7A` 的功能开发与阶段验收。
  - 新链类型、新 schema、新配置模型扩展。
  - 把当前 `Swap` 首批实现误写成 `Stage 6B` 全量闭环完成。

## 7. 当前任务拆解

### 历史 Stage 6A 收口记录

- `T6A-04`：已完成 `market-service` 收口与 Tiingo 受控自动化入口。
- `T6A-05`：已完成 `wallet-service` 收口与外部真节点受控自动化入口。
- `T6A-06`：已完成 `trading-core-service` 收口与交易主链路核对。
- `T6A-07`：已完成 `market.price.tick` Kafka 入口失败重试专项。
- `T6A-08`：已完成 `gateway / identity-service` 兼容性回归。
- `T6A-09`：已完成收口审计、阶段文档与统一接口文档同步。

### 当前 Stage 6B 首批实现

- 已完成 `market -> trading` 的 `Swap rate` owner Redis 共享快照交接。
- 已完成 `trading-core-service` 的本地定时结算、账本幂等落账与集成测试。
- 下一步顺序固定为：
  1. 查询展示路径拍板与实现。
  2. 是否需要 `Swap` Kafka 业务事件拍板与实现。
  3. 更完整运营观测与阶段验收。

## 8. 验收口径

- 本轮完成标准仍为：`代码实现完成 + 测试结论明确 + 文档同步完成`。
- 当前阶段结论统一为：
  - `Stage 6A` 主链路收口结论保持不变。
  - `Stage 6B` 已启动首批实现；`Swap rate` owner 共享与本地幂等结算已落地，但查询展示、业务事件与整体验收仍未完成。
- 当前已闭合的验证证据：
  - 目标模块编译通过。
  - `Swap` 交易侧定向集成测试通过。
  - README、启动手册、当前开发计划、测试规范与事务幂等规范已同步。

## 9. 当前风险与冻结项

- `README / 开发启动手册 / 当前开发计划 / 测试规范 / 事务与幂等规范` 若不同步，会造成 `Swap` owner 共享口径和阶段口径漂移。
- Tiingo 与 wallet 外部自动化入口仍依赖显式环境变量与 JVM trust store；缺失时不会形成可归档真跑证据。
- `Swap` 当前采用 owner Redis 快照；若未来扩展 JSON 结构，必须继续保证交易侧反序列化兼容。
- `Swap` 当前不发 Kafka 业务事件；若后续启用，必须补正式 payload 规范、Outbox 和消费者兼容验证。
- `Swap` 当前不做精确历史补算；超出 `rollover ± stale.max-age` 的后续报价不得拿来补算同一 `rollover`。

### SPEC-TRD-001 规则冻结结论

- 持仓模式正式冻结为 `One-Way + Isolated`。
- 单用户单 `symbol` 只允许存在一个净持仓。
- 手续费冻结为双边收费；一期先以 `t_symbol.taker_fee_rate` 作为统一动态费率。
- `Swap` 冻结为按 `rollover` 时点 fresh 有效价计算结算名义本金并乘以多/空费率；一期不引入三倍日和节假日顺延特殊规则。
- `Swap rate` owner 共享链当前实现为 `market-service -> Redis snapshot -> trading-core-service`。
- 上述内容是正式产品规则与当前实现口径，不等于 `Swap` 查询展示、业务事件和阶段验收已经完成。

## 10. 当前阻断与补证责任

- 当前阻断任务编号：无。
- 当前阻断缺口：无新增代码阻断；剩余缺口集中在 `Swap` 查询展示、业务事件决策、外部真源 / 真节点运行证据与后续阶段验收。
- 当前阶段结论：`Stage 6B` 首批 `Swap` 实现已落地，但不能标记为 `Stage 6B` 已完成。
- Builder：下一轮只在你拍板后继续推进查询展示或 Kafka 业务事件。
- QA：后续若扩展验收，需把 `Swap` 查询展示、外部真源 / 真节点联动与 JSON 兼容单独列为验证项。
- Boole：继续保持 `README / 开发启动手册 / 当前开发计划 / 测试规范 / 事务与幂等规范` 与实现一致。

## 11. 当前不应抢跑的事项

- 直接进入 `Stage 6C / 7 / 7A`。
- 在外部链节点与 Tiingo 真源仍未进入同一自动化用例前，把当前验证误写成“全平台联调完成”。
- 在 `Swap` 查询展示、业务事件和阶段验收尚未完成前，直接宣称隔夜利息已全部完成。
- 把当前系统表述为“生产可用”或“可对外公测”。
