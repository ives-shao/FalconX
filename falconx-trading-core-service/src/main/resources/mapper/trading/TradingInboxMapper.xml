<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.falconx.trading.repository.mapper.TradingInboxMapper">

    <insert id="insertProcessedIfAbsent" parameterType="com.falconx.trading.repository.mapper.record.TradingInboxRecord">
        INSERT IGNORE INTO t_inbox (
            id,
            event_id,
            event_type,
            source,
            payload,
            status,
            consumed_at,
            created_at,
            updated_at
        ) VALUES (
            #{id},
            #{eventId},
            #{eventType},
            #{source},
            CAST(#{payloadJson} AS JSON),
            #{statusCode},
            #{consumedAt},
            #{createdAt},
            #{updatedAt}
        )
    </insert>
</mapper>
